package CoreJava;

/*
1️⃣ Contract Rule (Most Important)
     - If two objects are equal using equals()
     - They must return the same hashCode()

2️⃣ Why It Exists
    -Because hash-based collections like:
        HashMap
        HashSet,
      Use it to decide bucket placement.

      Always override equals() and hashCode() together
 */

public class HashCodeBasics {
}
